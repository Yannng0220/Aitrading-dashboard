[build]
  command = "npm run build"
  publish = "dist"

# Local dev (optional): netlify dev will proxy functions at /.netlify/functions/*
[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/tickers"
  to = "/.netlify/functions/tickers"
  status = 200

